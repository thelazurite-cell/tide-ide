namespace TestAutomation.SolutionHandler.Core
{
    /// <summary>
    /// The navigation enum type - what type are we looking at within the hierarchy of items ?
    /// </summary>
    public enum NavigationItemType
    {
        Directory,
        File,
        Project,
        Solution
    }
}